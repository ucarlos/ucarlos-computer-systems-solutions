#include "../common_header.h"
/*
 * Write code for a function with the following prototype:
 *
 * Return 1 when x can be represented as an n-bit, 2's-complement num
 * 0 Otherwise
 * Assume 1 <= n <= w
 */
       
int fits_bits(int x, int n);

int main(void){


}
int 
